// Siempre que creamos un avariable y no le asignamos valkor se crea con el valor por defecto undefined

var nombre 
var apellido

// Desde ES6 se comenzo a usar más la comilla simple que la doble, no hay mucha diferencia

 // JS ya no es obligatorio poner el punto y coma al final, es opcional, pero ya no se usa mucho, también se puede usar en una sola línea
apellido = 'Cuéllar', nombre = 'Renato'

console.log('Hola '+nombre +' '+ apellido)